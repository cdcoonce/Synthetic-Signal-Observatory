# Synthetic Signal Observatory — example environment variables
#
# This repo reads configuration from environment variables (see
# `synthetic_signal_observatory/config.py`).
#
# Notes
# -----
# - This file is a TEMPLATE only. Do not commit a real `.env` with secrets.
# - The app auto-loads a repo-root `.env` (if present) when configuration is read.
#   Environment variables you already exported in your shell still take precedence.
# - To disable `.env` loading (e.g., in CI), set `SSO_DISABLE_DOTENV=1`.
#
# DuckDB file path (relative to repo root is fine)
SSO_DB_PATH=data/sso.duckdb

# Number of events generated per click in the UI
SSO_BATCH_SIZE=25

# RNG seed for deterministic synthetic generation
SSO_SEED=42

# Destructive control gate: enables the "Reset database" button in the UI.
# - 0/false/off = disabled (default)
# - 1/true/on   = enabled (still requires in-UI confirmation checkbox)
SSO_ALLOW_DB_RESET=1

# Disable repo-root `.env` loading.
# - 0 = enabled (default): `load_app_config()` will read `.env` if present
# - 1 = disabled: ignore `.env` entirely; only real environment variables + defaults apply
SSO_DISABLE_DOTENV=0

# -----------------------------------------------------------------------------
# Live Mode (Faux Real-Time Display)
# -----------------------------------------------------------------------------

# Refresh interval in seconds when Live Mode is enabled.
# Must be a positive integer (default:4).
SSO_AUTO_REFRESH_INTERVAL=4

# Whether Live Mode is enabled by default when the app starts.
# - 0/false = off (default): manual "Generate" button behavior
# - 1/true  = on: auto-generates events at the configured interval
SSO_AUTO_RUN_DEFAULT=0
